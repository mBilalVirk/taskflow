<div>
    <!-- Hero Section -->

    @include('components.public.hero')

    <!-- Features Section -->
    @include('components.public.features')

    <!-- Solutions Section -->
    @include('components.public.solutions')

    <!-- Pricing Section -->
    @include('components.public.pricing')

    <!-- CTA Section -->
    @include('components.public.cta')
</div>
